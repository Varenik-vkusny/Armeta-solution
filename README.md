# Детектор печатей, подписей и QR-кодов в документах

Этот проект представляет собой комплексное решение для автоматического обнаружения и выделения трех ключевых типов объектов на отсканированных документах или PDF-файлах: **печатей**, **подписей** и **QR-кодов**.

Система построена на базе моделей глубокого обучения (YOLOv8) и классических библиотек компьютерного зрения. Она способна обрабатывать как одиночные, так и пакетные PDF-файлы, генерируя на выходе обработанные изображения с выделенными объектами и JSON-файл с их координатами.

## 🚀 Основные возможности

*   **Интерактивный Web-интерфейс**: Удобная загрузка файлов через drag-and-drop прямо в браузере.
*   **Двухмодельная система для печатей и подписей**: Используется каскад из двух моделей YOLO для максимальной точности. Первая модель находит очевидные печати и подписи, а вторая, специализированная, ищет более сложные и "скрытые" подписи.
*   **Турбо-режим для QR-кодов**: Высокооптимизированный алгоритм для быстрого поиска QR-кодов, комбинирующий `pyzbar` и резервное сканирование с помощью YOLO.
*   **REST API и пакетная обработка**: Помимо веб-интерфейса, доступен API-эндпоинт для интеграции и скрипт (`orkestrator.py`) для локальной обработки папок.
*   **Детальный вывод**: Для каждого документа создается ZIP-архив с обработанными страницами (`.png`) и JSON-файлом `annotations.json` с координатами всех найденных объектов.

## 🛠️ Стек технологий

*   **Backend**: Python 3.10+, FastAPI
*   **Frontend**: HTML, CSS, JavaScript (Vanilla)
*   **AI / Computer Vision**:
    *   `ultralytics (YOLOv8)`: для детекции объектов.
    *   `OpenCV`: для обработки изображений.
    *   `pyzbar`: для декодирования QR-кодов.
*   **Работа с PDF**: `pdf2image`
*   **Внешние зависимости**: `Poppler` (требуется ручная установка).

## ⚙️ Подготовка к запуску

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/Varenik-vkusny/Armeta-solution.git
cd innovateX_Hack
```

### Шаг 2: Загрузка файлов моделей

1.  **Поместите все `.pt` файлы** в корневую папку проекта (`innovateX_Hack/`), чтобы они лежали рядом с `main.py`.

### Шаг 3: Установка зависимостей

1.  **Создайте и активируйте виртуальное окружение:**
    ```bash
    # Создание
    python -m venv venv
    # Активация (Windows)
    venv\Scripts\activate
    # Активация (macOS/Linux)
    source venv/bin/activate
    ```

2.  **Установите зависимости Python:**
    ```bash
    pip install -r requirements.txt
    ```

3.  **Установите Poppler**:
    Библиотека `pdf2image` требует наличия утилиты Poppler для работы с PDF.
    *   **Windows**: Скачайте архив с [официального сайта](https://github.com/oschwartz10612/poppler-windows/releases/), распакуйте и укажите путь к папке `bin` в переменной `POPPLER_PATH` в файле `orkestrator.py`.
    *   **macOS**: `brew install poppler`
    *   **Linux**: `sudo apt-get install poppler-utils`

## 🚀 Как использовать

### 1. Запуск веб-интерфейса (Основной способ)

1.  Убедитесь, что все зависимости установлены и виртуальное окружение **активировано**.
2.  Запустите сервер из корневой папки проекта:
    ```bash
    python -m uvicorn main:app --reload
    ```
3.  Откройте приложение в браузере по адресу: **[http://127.0.0.1:8000](http://127.0.0.1:8000)**

### 2. Локальная обработка папки (Альтернативный способ)

Этот режим полезен для пакетной обработки большого количества файлов без использования веб-интерфейса.

1.  Поместите PDF-файлы для анализа в папку `test`.
2.  Запустите скрипт-оркестратор:
    ```bash
    python orkestrator.py
    ```
3.  Результаты появятся в папке `processed_results`.

## 📂 Структура проекта

```
innovateX_Hack/
│
├── static/                     # Статические файлы (картинки для фронтенда)
├── templates/                  # HTML-шаблоны
│   └── index.html
├── .gitignore                  # Файл для исключения из Git
├── best.pt                     # Модель YOLO (нужно скачать отдельно)
├── signatures.pt               # Модель YOLO (нужно скачать отдельно)
├── stamp_and_sings_detector.pt # Модель YOLO (нужно скачать отдельно)
├── main.py                     # FastAPI-сервер и API-логика
├── orkestrator.py              # Скрипт для пакетной обработки
├── qr_detector.py              # Логика детекции QR-кодов
├── requirements.txt            # Список зависимостей Python
└── stamps_and_signs_detector.py# Логика детекции печатей и подписей
```
